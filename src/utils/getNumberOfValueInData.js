// export function getNumberOfValueInData(value, data) {
//   const number = data.reduce((total, item) => {
//     return total + (item.value === stateName ? 1 : 0);
//   }, 0);
//   return number;
// }